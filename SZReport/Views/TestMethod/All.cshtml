@model List<List<string>>
@{
    Layout = null;
}

<div class="row">
    <div class="col-md-4">
        @foreach (var a in Model)
        {
            <div class="col-md-6">
                @foreach (var s in a)
                {
                    <div>@s</div>
                }
            </div>
        }
    </div>
</div>>